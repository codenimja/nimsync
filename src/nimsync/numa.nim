## Compatibility shim — this module has moved to nimsync/experimental/numa
## Existing imports (e.g. `import nimsync/numa`) continue to work.
## Prefer: `import nimsync/experimental/numa`

import nimsync/experimental/numa
export numa
