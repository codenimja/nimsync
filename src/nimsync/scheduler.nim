## Compatibility shim — this module has moved to nimsync/experimental/scheduler
## Existing imports (e.g. `import nimsync/scheduler`) continue to work.
## Prefer: `import nimsync/experimental/scheduler`

import nimsync/experimental/scheduler
export scheduler
