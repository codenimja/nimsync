## Compatibility shim — this module has moved to nimsync/experimental/errors
## Existing imports (e.g. `import nimsync/errors`) continue to work.
## Prefer: `import nimsync/experimental/errors`

import nimsync/experimental/errors
export errors
