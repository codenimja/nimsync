## Compatibility shim — this module has moved to nimsync/experimental/backpressure
## Existing imports (e.g. `import nimsync/backpressure`) continue to work.
## Prefer: `import nimsync/experimental/backpressure`

import nimsync/experimental/backpressure
export backpressure
