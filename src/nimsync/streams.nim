## Compatibility shim — this module has moved to nimsync/experimental/streams
## Existing imports (e.g. `import nimsync/streams`) continue to work.
## Prefer: `import nimsync/experimental/streams`

import nimsync/experimental/streams
export streams
