## Compatibility shim — this module has moved to nimsync/experimental/supervision
## Existing imports (e.g. `import nimsync/supervision`) continue to work.
## Prefer: `import nimsync/experimental/supervision`

import nimsync/experimental/supervision
export supervision
