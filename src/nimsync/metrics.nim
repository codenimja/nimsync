## Compatibility shim — this module has moved to nimsync/experimental/metrics
## Existing imports (e.g. `import nimsync/metrics`) continue to work.
## Prefer: `import nimsync/experimental/metrics`

import nimsync/experimental/metrics
export metrics
