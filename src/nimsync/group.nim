## Compatibility shim — this module has moved to nimsync/experimental/group
## Existing imports (e.g. `import nimsync/group`) continue to work.
## Prefer: `import nimsync/experimental/group`

import nimsync/experimental/group
export group
